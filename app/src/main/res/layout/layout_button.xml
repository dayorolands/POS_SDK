<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="android.view.View"/>

        <variable
            name="text"
            type="String" />

        <variable
            name="src"
            type="android.graphics.drawable.Drawable" />

        <!--<variable-->
            <!--name="id"-->
            <!--type="Integer" />-->

        <variable
            name="buttonHandler"
            type="com.appzonegroup.app.fasttrack.utility.ButtonHandler" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/rectangle"
        android:onClick="@{buttonHandler::onClick}"
        android:orientation="vertical"
        android:padding="@dimen/ten_dp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:padding="30dp"
            android:src="@{src, default=@drawable/ic_logo_with_name}" />

        <com.appzonegroup.app.fasttrack.ui.TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@{text, default=Button}"
            android:textAlignment="center"
            android:textAllCaps="true"
            android:textColor="@color/blue" />
    </LinearLayout>
</layout>
