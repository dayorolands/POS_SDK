<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#fff"
        tools:context=".ReprintAnyActivity">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <include
                android:id="@+id/header"
                layout="@layout/header"
                app:hideBackButton='@{false}'
                app:subtitle='@{"Reprint Any"}' />

            <LinearLayout
                android:id="@+id/cont"
                style="@style/AppTheme.MenuLayout"
                android:layout_below="@id/header">

                <com.google.android.flexbox.FlexboxLayout style="@style/AppTheme.MenuFlexLayout">

                    <include
                        android:id="@+id/reprint_last_button"
                        layout="@layout/card_menu_button"
                        app:src='@{@drawable/ic_purchase}'
                        app:text='@{"Reprint Last"}' />

                    <include
                        android:id="@+id/reprint_by_stan_button"
                        layout="@layout/card_menu_button"
                        app:src='@{@drawable/ic_reversal}'
                        app:text='@{"Reprint By STAN"}' />

                </com.google.android.flexbox.FlexboxLayout>
            </LinearLayout>

        </RelativeLayout>
    </ScrollView>
</layout>